<p id="notice"></p>
<div class="container">
  <div class="row">
    <div class="col-md-3 offset-md-1">
    <h2>User info</h2>
      <%= image_tag @user.get_profile_image(100,100) %>

<table class="t">
  <tr>
  <h6>name</h6>
    <%= @user.name %>
  </tr>
  <tr>
  <h6>introduction</h6>
    <%= @user.introduction %>
  </tr>
</table>
  <%= link_to edit_user_path(@user.id), class: "btn btn-outline-secondary btn-block" do %>
  <i class="fas fa-user-cog"></i>
  <% end %>

<%= form_with model:@book_new do |f| %>
  <div class="container px-5 px-sm-0">
    <h2 class="mt-3">New book</h2>
  <h4>Title</h4>
    <%= f.text_field :title,class: "col-md-12" %>
  <h4>Opinion</h4>
    <%= f.text_field :body,class: "col-md-12" %>
  <nav>
    <%= f.submit 'Create Book',class: "col-md-12 btn btn-primary" %>
  </nav>
<% end %>
    </div>
  </div>

  <div class="col-md-8 offset-md-1">
    <h2>Book detail</h2>
  <table class="table">
    <tboby>
      <tr>
      <td>
        <%= link_to user_path(@book.user.id) do %>
        <%= image_tag @book.user.get_profile_image(100,100) %>
        <br>
        <%= @book.user.name %>
      </td>
      <td>
        <%= @book.title %>
      </td>
      <td>
        <%= @book.body %>
      </td>
  <% end %>

  <% if @book.user == current_user %>
  <%= link_to "Edit", edit_book_path(@book) %>
  <%= link_to "Destroy", book_path(@book), method: :delete, "data-confirm" => "Do you really want to delete this？" %>
  <% end %>
      </tr>
  </tboby>
</table>